# LotMonitor 数据分析与 MDP 控制方案

## 一、当前数据质量分析

### 1.1 数据问题

从采集的数据来看，存在以下问题:

| 问题 | 现象 | 原因 |
|------|------|------|
| 大量空样本 | ~95% 样本 RTT=0 | 服务端连接(443端口)没有主动发送数据 |
| RTT 异常 | curr_rtt=971ms (正常应<100ms) | 可能是 RTO 超时后的重传 RTT |
| bytes_acked=0 | 无法计算吞吐量 | 序号跟踪bug (已修复) |
| throughput=0 | 所有样本 | 依赖 bytes_acked |

### 1.2 有效数据分析

```
连接: 74.48.174.95:55982 -> 204.44.191.97:8080
min_rtt:     2,348 us  (2.3 ms)
curr_rtt:  971,875 us  (971 ms) ← 异常高
srtt:      123,538 us  (123 ms)
loss_count: 1703 → 4692 (递增)
loss_rate:  28,144 ppm (2.8%)
```

**结论**: 这是一个高丢包、高延迟的连接，正在经历严重拥塞。

### 1.3 数据有效性评估

| 特征 | 可用性 | 说明 |
|------|--------|------|
| min_rtt | ✅ 可用 | 基线 RTT |
| curr_rtt | ⚠️ 部分 | 需过滤异常值 |
| srtt | ✅ 可用 | 平滑后更稳定 |
| queue_delay | ✅ 可用 | curr_rtt - min_rtt |
| loss_count | ✅ 可用 | 累计丢包 |
| loss_rate | ✅ 可用 | 丢包率 |
| bytes_acked | ❌ 需修复 | 序号跟踪问题 |
| throughput | ❌ 需修复 | 依赖 bytes_acked |
| inflight | ⚠️ 部分 | 需正确的序号 |

---

## 二、Netfilter 控制能力分析

### 2.1 Netfilter 能做什么

```
┌─────────────────────────────────────────────────────────────┐
│                    Netfilter 能力范围                        │
├─────────────────────────────────────────────────────────────┤
│ ✅ 可以做:                                                   │
│   • 读取/修改 TCP 头部字段 (窗口大小、标志位等)               │
│   • 丢弃数据包 (NF_DROP)                                     │
│   • 延迟转发 (通过队列)                                      │
│   • 生成/注入新数据包 (如重复ACK)                            │
│   • 修改通告窗口 (rwnd) 间接影响发送速率                     │
│                                                              │
│ ❌ 不能做:                                                   │
│   • 直接修改 cwnd (拥塞窗口) - 内核内部状态                  │
│   • 直接修改 ssthresh (慢启动阈值)                           │
│   • 访问 tcp_sock 结构体                                     │
│   • 调用 TCP 拥塞控制回调函数                                │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 间接控制方法

虽然不能直接控制 cwnd，但可以通过以下方式间接影响:

```c
// 方法1: 修改通告窗口 (rwnd)
// 发送方的有效窗口 = min(cwnd, rwnd)
// 降低 rwnd 可以限制发送速率
th->window = htons(new_rwnd);

// 方法2: 生成重复ACK触发快速重传
// 3个重复ACK会触发 cwnd 减半
inject_dup_ack(conn, 3);

// 方法3: 人为丢包
// 触发RTO超时，cwnd 降到1
return NF_DROP;
```

### 2.3 控制能力对比

| 方法 | 控制精度 | 响应速度 | 副作用 |
|------|----------|----------|--------|
| 修改 rwnd | 中 | 快 | 可能影响对方缓冲区估计 |
| 注入重复ACK | 低 | 中 | 只能触发减半 |
| 丢包 | 低 | 慢 | 严重影响性能 |

**结论**: Netfilter 只能进行粗粒度的间接控制，不适合精确的 MDP 控制。

---

## 三、推荐方案: eBPF TCP 拥塞控制

### 3.1 为什么选择 eBPF

```
┌─────────────────────────────────────────────────────────────┐
│                    eBPF struct_ops                          │
├─────────────────────────────────────────────────────────────┤
│ 优势:                                                        │
│   ✅ 直接访问 tcp_sock 结构 (cwnd, ssthresh, rtt 等)        │
│   ✅ 实现完整的 tcp_congestion_ops 回调                     │
│   ✅ 内核级性能，用户空间开发便利性                         │
│   ✅ 动态加载/卸载，无需重启                                │
│   ✅ 安全沙箱，不会导致内核崩溃                             │
│                                                              │
│ 要求:                                                        │
│   • Linux 5.6+ (struct_ops 支持)                            │
│   • 推荐 Linux 5.15+ (更完善的 TCP 支持)                    │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                      用户空间                                │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │ 训练进程    │    │ 推理引擎    │    │ 策略更新    │     │
│  │ (离线训练)  │ →  │ (ONNX/TF)  │ →  │ (BPF Map)   │     │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
└────────────────────────────┬────────────────────────────────┘
                             │ BPF Maps (状态/动作)
                             ↓
┌─────────────────────────────────────────────────────────────┐
│                      内核空间                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  eBPF TCP Congestion Control (struct_ops)           │   │
│  │  ├── cong_control()  ← 主要决策点                   │   │
│  │  ├── ssthresh()      ← 丢包时阈值计算               │   │
│  │  ├── undo_cwnd()     ← 误判恢复                     │   │
│  │  └── pkts_acked()    ← ACK 到达处理                 │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 MDP 状态空间设计

```python
# 状态向量 (可从 tcp_sock 直接获取)
state = {
    # RTT 相关 (归一化)
    'rtt_ratio': curr_rtt / min_rtt,      # 延迟膨胀比
    'rtt_gradient': d(rtt)/dt,            # RTT 变化率
    'rtt_variance': rtt_var / srtt,       # RTT 抖动

    # 带宽相关
    'bdp_ratio': inflight / (bw * min_rtt), # BDP 利用率
    'delivery_rate': bytes_acked / interval, # 交付速率

    # 丢包相关
    'loss_rate': lost / sent,             # 丢包率
    'ecn_ratio': ecn_ce / total,          # ECN 标记率

    # 窗口相关
    'cwnd_utilization': inflight / cwnd,  # 窗口利用率
}
```

### 3.4 动作空间设计

```python
# 动作: cwnd 调整因子
actions = [
    0.5,    # 大幅减少 (拥塞严重)
    0.75,   # 中等减少
    0.9,    # 小幅减少
    1.0,    # 保持
    1.05,   # 小幅增加 (谨慎探测)
    1.1,    # 中等增加
    1.25,   # 大幅增加 (带宽充足)
]

# 应用动作
new_cwnd = clamp(cwnd * action, min_cwnd, max_cwnd)
```

---

## 四、实现方案

### 方案 A: 纯 eBPF 实现 (推荐)

**优点**: 高性能，低延迟
**缺点**: eBPF 指令限制，无法运行复杂模型

```
训练 (离线)           →  量化  →  查表 (在线)
PyTorch Q-Network         →      BPF Map 查表
```

### 方案 B: 用户空间决策 + eBPF 执行

**优点**: 可运行复杂模型
**缺点**: 增加延迟 (~100us per decision)

```
eBPF 采集状态 → BPF Map → 用户空间推理 → BPF Map → eBPF 执行
```

### 方案 C: Netfilter 监控 + setsockopt 控制

**优点**: 无需 eBPF，兼容性好
**缺点**: 只能控制应用层 socket，粒度粗

```python
# 用户空间控制示例
import socket

sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
# 设置 TCP 拥塞控制算法
sock.setsockopt(socket.IPPROTO_TCP, socket.TCP_CONGESTION, b'cubic')
# 设置发送缓冲区 (间接影响窗口)
sock.setsockopt(socket.SOL_SOCKET, socket.SO_SNDBUF, 65536)
```

---

## 五、推荐实施路径

### Phase 1: 数据采集完善 (当前)

1. 修复 lotmonitor.c 的序号跟踪问题 ✅
2. 确保 bytes_acked 和 throughput 正确
3. 采集足够的训练数据 (多种网络条件)

### Phase 2: 离线训练

1. 使用 Q-Learning/DQN 训练策略
2. 验证策略在仿真环境中的表现
3. 量化模型为查表形式

### Phase 3: eBPF 实现

1. 实现 eBPF struct_ops 框架
2. 集成量化后的策略
3. 在受控环境测试

### Phase 4: 生产部署

1. A/B 测试与 CUBIC/BBR 对比
2. 渐进式部署
3. 持续监控和调优

---

## 六、代码示例

见以下文件:
- `ebpf_cc_demo.c` - eBPF 拥塞控制示例
- `mdp_train.py` - MDP 训练脚本
- `realtime_control_demo.py` - 实时控制演示
